<view class="container">
  <!-- 宝宝列表 -->
  <view class="baby-list" wx:if="{{babies.length > 0}}">
    <view class="baby-item" wx:for="{{babies}}" wx:key="id">
      <view class="baby-info" bindtap="switchBaby" data-id="{{item.id}}">
        <view class="baby-avatar">
          <image src="{{item.avatarUrl || '/images/default-baby.png'}}" mode="aspectFill"></image>
          <view class="current-badge" wx:if="{{currentBaby && currentBaby.id === item.id}}">当前</view>
        </view>
        <view class="baby-details">
          <text class="baby-name">{{item.nickname}}</text>
          <text class="baby-birthday">生日：{{item.birthday}}</text>
        </view>
      </view>
      
      <view class="baby-actions">
        <view class="action-btn edit" bindtap="editBaby" data-id="{{item.id}}">编辑</view>
        <view class="action-btn delete" bindtap="deleteBaby" data-id="{{item.id}}">删除</view>
      </view>
    </view>
  </view>

  <!-- 空状态 -->
  <view class="empty-state" wx:else>
    <view class="empty-icon">👶</view>
    <view class="empty-text">暂无宝宝信息</view>
    <view class="empty-desc">添加第一个宝宝开始评估吧</view>
  </view>

  <!-- 添加按钮 -->
  <view class="add-section">
    <button class="add-btn" bindtap="addBaby">
      <view class="add-icon">+</view>
      <text class="add-text">添加宝宝</text>
    </button>
  </view>
</view> 